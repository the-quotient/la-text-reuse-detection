name: ml-lang-models
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - numpy
  - pandas
  - scikit-learn
  - matplotlib
  - jupyter
  - pytorch=1.9.0
  - cudatoolkit=10.2  # Optional: remove if not using GPU
  - pip:
      - transformers
      - sentence-transformers
      - datasets
      - accelerate
      - tensorboard

